[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "logbookpics"
authors = [{name = "Elettra Sincrotrone Trieste", email = "todo"}]
dynamic = ["version", "description"]
